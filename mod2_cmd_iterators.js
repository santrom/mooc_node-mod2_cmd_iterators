// Primer apartado
console.log();
console.log("Route	to	node.js: " + process.argv[0]); // Muestra ruta del interprete
console.log("Route	to	this	file: " + process.argv[1]); // Muestra fichero nombre del fichero .js
console.log();

// Segundo apartado
let [a,b,...arrayParametros];
//for arrayParametros.forEach(elemento,i < arrayParamentros.length, i++) => {
for arrayParametros.forEach(elemento) {
		if (elemento == "-r") {
			
console.log("Existe elemento");
		}
}

/*
array_0.forEach((elem, i) => {
  if (elem==="-r") { 
    array_0.splice(i,2);
  }
});
*/